@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card shadow-sm">
                <div class="card-body text-center">
                    <div class="mb-4">
                        <i class="bi bi-shield-lock" style="font-size: 5rem; color: #dc3545;"></i>
                    </div>
                    
                    <h1 class="card-title mb-3">Acceso Denegado</h1>
                    <p class="card-text mb-4">
                        Lo sentimos, no tienes permisos para acceder a esta sección.
                        Solo los administradores del sistema pueden utilizar el panel de administración.
                    </p>
                    
                    <div class="d-grid gap-2">
                        <a asp-controller="Home" asp-action="Index" class="btn btn-primary">
                            <i class="bi bi-house"></i> Ir a Inicio
                        </a>
                        <a asp-controller="Usuarios" asp-action="GestionUsuarios" class="btn btn-outline-secondary">
                            <i class="bi bi-people"></i> Gestionar Mis Usuarios
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 